The Task: 
Create a tool that takes a string and a command (like uppercase, lowercase, or reverse) and prints the result.

Need to take cammands:
    we can get the arguments by using std::env::args crate

